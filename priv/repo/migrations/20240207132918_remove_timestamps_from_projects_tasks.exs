defmodule Candone.Repo.Migrations.RemoveTimestampsFromProjectsTasks do
  use Ecto.Migration

  def change do
    # alter table(:projects_tasks) do
      # remove :inserted_at
      # remove :updated_at
    # end
  end
end
